---
interface Props {
    name: string;
    color: string;
}

const { name, color = "white" } = Astro.props;
---

<div
    class=`flex gap-2 items-center justify-center py-2 px-4 border border-${color}`
>
    <slot name="icon" />

    <p>{name}</p>
</div>
