---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="my-2 border border-catpuccin-lavender p-4">
    <p class="text-center">{title}</p>
    <div class="flex flex-wrap items-center gap-4 p-4">
        <slot />
    </div>
</div>
